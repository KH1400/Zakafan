services:
  web:
    image: docker-registry.zakafan.ir/dynograph-front
    # build:
    #   context: .
    #   dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - 3000:3000
    volumes:
      - "./public/uploads:/app/public/uploads"